interface:
  display_name: "Verify Instrumentation Coverage"
  short_description: "Audit tagging coverage from memory and traffic"
  default_prompt: "Use $verify-instrumentation to compare product memory and available traffic with current tags, then offer an optional shareable HTML report."
